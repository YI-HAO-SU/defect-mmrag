# Core ML
torch>=2.4.0
transformers>=4.45.0
accelerate>=0.34.0
pillow>=10.0.0

# Inference engine — install separately if CUDA mismatch:
#   pip install vllm
vllm>=0.6.3

# Vector DB
qdrant-client>=1.11.0

# LLM client (OpenAI-compatible)
openai>=1.50.0

# Web app
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Utilities
pydantic>=2.0
pydantic-settings>=2.0
numpy>=1.26
tqdm>=4.66

# Dev / testing
pytest>=8.0
